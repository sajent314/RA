import React from 'react';
import { View, Text } from 'react-native';

export const Camera = () => {
  return (
    <View style={{ flex: 1, justifyContent: 'center', alignItems: 'center' }}>
      <Text>Camera is not available on the web.</Text>
    </View>
  );
};
